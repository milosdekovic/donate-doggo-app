A playful web app where users can choose dogs to 'donate' by adding them to a virtual cart, with the functionality implemented using Zustand and Stripe.

## Getting Started

First, run the development server:

```bash
npm install
npm run dev

